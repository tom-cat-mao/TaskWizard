Pillow>=12.0.0
openai>=2.9.0
nicegui>=3.0.0
fastapi>=0.115.0

# For LangGraph Agent Architecture
langchain>=1.0.0
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=1.0.0
langchain-anthropic>=1.0.0
langchain-google-genai>=4.0.0

# For iOS Support
requests>=2.31.0
sqlite-vec>=0.1.9
mlx-embeddings==0.1.0
pypinyin>=0.55.0

# For Model Deployment

## After installing sglang or vLLM, please run pip install -U transformers again to upgrade to 5.0.0rc0.
## Any dependency conflicts related to Transformers can be ignored.

# sglang>=0.5.6.post1
# vllm>=0.12.0
# transformers>=5.0.0rc0

# Optional Visual Grounding (LocateAnything on Apple Silicon MLX)
# mlx-vlm is intentionally kept out of default requirements so CI and non-MLX
# hosts do not need Apple Silicon/Metal-only dependencies. Install a compatible
# mlx-vlm build manually in the local MLX environment before running the real
# LocateAnything provider or benchmark.

# Development & Testing
pytest>=7.0.0
black>=23.0.0
mypy>=1.0.0
ruff>=0.1.0
